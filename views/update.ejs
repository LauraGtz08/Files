<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title> UPDATE </title>
        <link rel="stylesheet" href="css/mystyles.css">
    </head>
    <body>
        <form class="box" action="/save_note" method="post">
           <div class="field">
               <label for="POST-name" class="label">Name: </label>
                    <div class="control">
                        <input class="input" type="hidden" name="name" value="<%= result.name %">
                    </div>
            </div>

            <div class="field">
                <label for="POST-new-name" class="label">New name: </label>
                    <div class="control">
                        <input class="input" type="text" name="nname" value="<%= result.nname %">
                    </div>
            </div>

            <textarea name="ncontent" rows="10" cols="50" value="<%= result.ncontent %"></textarea>
            <button type="submit" class="button is-rounded is-black">Save</button>
            </div>
        </form>
    </body>
</html>